-- Database initialisation script
-- Hibernate will auto-create tables, but we ensure extensions and defaults exist.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- The following tables are managed by Hibernate (ddl-auto: update).
-- This script is a no-op except for enabling uuid generation support.
